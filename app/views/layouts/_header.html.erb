<header class="border-b border-gray-800 border-solid">
    <div class="px-8 py-6 flex justify-between">
        <nav class="flex flex-row gap-x-4">
            <a href="<%= root_path %>">
                <span class="text-white text-2xl font-bold">Home</span>
            </a>
            <a href="<%= people_path %>">
                <span class="text-white text-2xl font-bold">People of Thailand</span>
            </a>
        </nav>
        <%= render 'layouts/user_navbar'%>
    </div>
</header>