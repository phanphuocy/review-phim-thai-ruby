<header class="bg-gray-900">
    <div class="px-4 py-6 flex justify-between">
        <nav>
            <a href="<%= root_path %>">
                <span class="text-white text-2xl font-bold">REVIEW PHIM THAI</span>
            </a>
        </nav>
        <%= render 'layouts/user_navbar'%>
    </div>
</header>